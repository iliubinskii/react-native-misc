export enum NavigationEvent {
  blur = "blur",
  focus = "focus"
}

export enum TabNavigatorBackBehavior {
  firstRoute = "firstRoute",
  history = "history",
  initialRoute = "initialRoute",
  none = "none",
  order = "order"
}
